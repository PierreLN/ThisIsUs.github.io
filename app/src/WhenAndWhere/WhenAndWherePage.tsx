import styles from "./WhenAndWherePage.module.css";
import Card from "../UI/Card";

const WhenAndWherePage = (props: any) => {
  return (
    <Card>
      Save the date and mark your calendars! We are thrilled to invite you to
      celebrate our union on [Date] at the enchanting [Venue]. Located in the
      heart of [Location], this picturesque setting will serve as the backdrop
      to our heartfelt vows and the beginning of our forever. Get ready to
      witness the love that brought us together and the promises we'll make to
      each other.
    </Card>
  );
};

export default WhenAndWherePage;
